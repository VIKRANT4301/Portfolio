import React from 'react';
import '../App.css'


const Projects = () => (
  <section>
    <h2>Projects</h2>
    <p>Project listing will go here (we can fetch from backend later).</p>
  </section>
);

export default Projects;
